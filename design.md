# 产品与技术设计

## 产品目标

“做个决定”在离线环境提供转盘、抽签、指尖、硬币、是或否和随机数字工具。
本轮目标是在不改变功能、状态机和本地数据契约的前提下还原批准设计稿。

## 页面信息架构

| 页面 | 职责 | 主要状态 |
| --- | --- | --- |
| 首页 | 六个工具入口、四个快捷模板、历史与设置入口 | phone 两列，宽屏增加列数 |
| 转盘/抽签 | 编辑候选项并随机决策 | 空闲、动画、结果、保存模板 |
| 指尖/硬币/是或否/随机数字 | 快速决策 | 空闲、运行、结果、统计 |
| 历史 | 筛选、日期分组、查看、复用和删除 | 空态、列表、详情 |
| 设置 | 反馈、主题、数据和关于 | 开关、主题分段、确认弹窗 |

## 视觉系统

- 风格：冷白、轻量、原生、紧凑。
- 主色：`#2F9BF3`，正文深蓝黑，辅助文字灰蓝。
- phone 卡片采用 12vp 圆角、细边框和低强度阴影。
- 插画和入口图标从用户批准 JPG 确定性裁切，参数见 `docs/ui/asset-manifest.md`。

## 多设备适配

- phone 竖屏是逐像素目标，页面基准宽度约 440vp。
- 首页使用 `ResponsiveLayout` 在宽窗口增加列数，内容设置最大宽度。
- 内页保留 `Scroll`/`List` 和相对宽度，避免固定内容宽度导致横向溢出。
- tablet 仅完成代码级响应式约束和单元测试，实际设备 QA 仍为 `blocked`。

## 状态与数据

- `domain/`、`stores/`、`services/`、Preferences 和导航状态机保持原有职责。
- UI 仅消费现有状态并调用原回调，不新增远程服务或持久化字段。
- 历史筛选只影响内存渲染列表，不写入 Preferences。

## 技术取舍与风险

- 源图自带浅色背景，可最大程度还原 light 主题；dark 主题不作为本轮像素基准。
- 项目现有模型、常量和工具目录职责清楚，不为满足 `common/` 建议而高风险搬迁。
- 不新增 `2in1` 设备声明，避免在未验证时扩大发布范围。
