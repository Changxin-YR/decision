# 产品与技术设计

## 产品目标

“做个决定”在离线环境提供转盘、抽签、指尖、硬币、是或否和随机数字工具。
本轮目标是在不改变功能、状态机和本地数据契约的前提下还原批准设计稿。

## 页面信息架构

| 页面 | 职责 | 主要状态 |
| --- | --- | --- |
| 首页 | 六个工具入口、四个快捷模板、历史与设置入口 | phone 两列，宽屏增加列数 |
| 转盘/抽签 | 编辑候选项并随机决策 | 空闲、动画、结果、保存模板 |
| 指尖/硬币/是或否/随机数字 | 快速决策 | 空闲、运行、结果、统计 |
| 历史 | 筛选、日期分组、查看、复用和删除 | 空态、列表、详情 |
| 设置 | 反馈、主题、数据和关于 | 开关、主题分段、确认弹窗 |

## 视觉系统

### 2026-08-03 双首页与决策反馈契约

- 浅色和深色首页是两套独立设计。浅色继续使用任务开始前的获批首页资源；深色使用用户参考图 `08ABFEBD290DA393EA2384CF1BED373A4B7864C35537A666890891C9BE236282` 派生资源，运行时使用独立 `Image` 节点切换。
- 转盘指针轴线为画布 `50%`，箭头尖端指向十二点白色分隔线。中心装饰不使用会显示矩形边界的 ArkUI 阴影。
- 深色硬币资源必须保持单一完整椭圆轮廓，不允许孤立像素、地面光线切口或叠加结果徽标。
- SoundPool 使用 `STREAM_USAGE_GAME`。过程音的播放请求完成后才开始动画计时；结果音的播放请求完成后才更新可见结果；新声音先停止旧声音，页面取消/后台切换会停止当前声音，生命周期请求代次防止旧释放覆盖新初始化。
- `DecisionTiming` 是统一时间契约：是/否 `1400ms`、硬币结果 `2500ms`（动画 `2300ms`）、抽签 `3000ms`、罗盘 `3600ms`、指尖选择 `3000ms`。

### 2026-08-03 深色页面覆盖补丁

- 深色首页工具与快捷模板、抽签、指尖选择和硬币展示使用独立深色资源；浅色模式继续引用原资源。
- 抽签与指尖深色插画使用完整深海军蓝画布，避免白底、透明化缺口和硬切残影；仅深色模式增加 18vp 圆角以柔化画布边界。
- 随机数字输入文字在深色模式固定为 `#FFFFFF`，不依赖平台输入框默认色；浅色模式继续消费正文令牌。
- 深色共享描边使用低对比冷蓝 `#203654`，浅色描边保持 `#E6EBF2`；页面结构、业务状态和数据职责不变。

### 2026-08-03 夜间模式视觉还原

- 深色主题使用 #071426 深海军蓝背景、#101E35 表面、#0C1930 输入底色和 #203654 描边；浅色主题令牌保持原值。
- 复用卡片、输入、页面标题、主/次按钮、选项编辑器和转盘均消费主题视觉令牌；不改变页面路由、交互回调、状态机或持久化。
- phone 竖屏仍为目标，现有滚动、安全区与 720vp 内容宽度限制保持不变。

- 风格：冷白、轻量、原生、紧凑。
- 主色：`#2F9BF3`，正文深蓝黑，辅助文字灰蓝。
- phone 卡片采用 12vp 圆角、细边框和低强度阴影。
- 插画和入口图标从用户批准 JPG 确定性裁切，参数见 `docs/ui/asset-manifest.md`。

## 多设备适配

- phone 竖屏是逐像素目标，页面基准宽度约 440vp。
- 首页使用 `ResponsiveLayout` 在宽窗口增加列数，内容设置最大宽度。
- 内页保留 `Scroll`/`List` 和相对宽度，并统一限制为居中的 720vp 最大内容宽度，避免 tablet 上横向拉满或溢出。
- 所有实际滚动容器使用隐藏滚动条与 Spring 边界反馈；即使内容不足一屏，拖动到边界也会给出回弹反馈。
- 主窗口使用沉浸式布局，通过 `TYPE_SYSTEM` 与 `TYPE_NAVIGATION_INDICATOR` 获取并监听动态避让区；根页面使用真实 px 值为上下边界留出空间。
- 安全区由窗口 Ability 发布到 `AppStorage`，页面只负责消费，不侵入各工具的业务状态或交互回调。
- `SystemBarService` 根据应用主题与系统颜色模式统一设置透明系统栏的内容色；设置页保存主题后即时同步，系统配置变化由 Ability 转发。
- tablet 已完成代码级响应式宽度、安全区和滚动反馈约束；实际设备 QA 仍为 `blocked`。

## 应用图标

- AppScope 与 EntryAbility 使用同名 `layered-image` 描述文件，前景和背景分别为独立的 1024×1024 PNG。
- 用户提供 PNG 的棋盘格是实际像素而非 Alpha；生产前景仅保留获批图标主体与透明轮廓，不把棋盘格打包进应用。
- 背景层为不透明 RGB 满画布，不预裁圆角；前景层为 RGBA，未额外增加内间距。
- 用户指定源文件 SHA-256 为 `F439622F74ED26F23140BB12E76B73365677593111EE357C005C34D9E152FC9B`，资源映射记录在 `docs/ui/asset-manifest.md`。

## 状态与数据

- `domain/`、`stores/`、`services/`、Preferences 和导航状态机保持原有职责。
- UI 仅消费现有状态并调用原回调，不新增远程服务或持久化字段。
- 历史筛选只影响内存渲染列表，不写入 Preferences。

## 反馈能力

- 设置页通过 `FeedbackSettingsController` 先持久化开关，再在开启成功时预览一次对应反馈；关闭开关或保存失败不触发预览，避免界面状态与实际设置不一致。
- 短音效由 `SoundService` 统一读取设置并委托 `SoundPoolBackend` 播放。应用启动时预加载 rawfile WAV，进入后台释放资源，回到前台按需重新初始化。
- 转盘、抽签、硬币、是或否、指尖选择和随机数字均在结果确定后调用统一结果音效；页面仍负责交互时机，随机规则和结果持久化职责保持不变。
- 声音或振动系统能力不可用时不改变决策结果；设置页会显示可见提示，工具页仅记录非阻断日志。
- 默认设置为振动开启、音效关闭；用户可在设置页独立开启音效，持久化后的值不被默认值覆盖。
- 第二版版本为 `1.0.1` / versionCode `1000001` / buildVersion `2`，target 和 compatible SDK 均为 HarmonyOS `6.0.2(22)`，runtimeOS 为 `HarmonyOS`。

## 指尖选择页导航与触控区

- `FingerSelectPage` 使用共享 `PageHeader` 的卡片式返回键语言；该组件默认维持 48vp，页面可通过可选属性使用 44vp 紧凑尺寸。
- 页面主体使用独立、可伸展的触控层承接多指识别，页首返回键与底部功能键在该层外，避免交互冲突。
- 根 `Navigation` 已动态消费系统底部避让区；移除小提示后，功能键仅保留 8vp 的内容底边距，因此在浅色、深色和动态导航区下均接近下边框但不侵入系统区域。

## 技术取舍与风险

- 源图自带浅色背景，可最大程度还原 light 主题；dark 主题不作为本轮像素基准。
- 项目现有模型、常量和工具目录职责清楚，不为满足 `common/` 建议而高风险搬迁。
- 不新增 `2in1` 设备声明，避免在未验证时扩大发布范围。
- foldable/tablet 的系统栏高度与姿态变化依赖动态避让区，不使用固定机型数值；当前只有 phone API 24 模拟器运行证据。
