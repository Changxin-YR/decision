# 变更记录

## 2026-08-03 SDK 与默认反馈复核

- 确认发布配置的 target/compatible SDK 均为 `6.0.2(22)`，运行目标为 `HarmonyOS`；标准检查同步以 API 22 原生 HarmonyOS 契约校验。
- 保留系统 `physicalFeedback` 振动反馈，默认开启；新安装、清除全部数据和损坏设置回退时的音效默认改为关闭。
- 声音服务测试显式开启音效后再验证播放，避免将默认设置与用户主动启用后的播放能力混为一谈。

## 2026-08-03 视觉修复与五工具反馈时序统一

- 浅色首页继续使用任务开始前的获批资源；深色首页独立使用用户最新参考派生资源，两套首页不再共用主题切换节点。
- 转盘指针固定在 `50%` 圆心轴并指向十二点分隔线；移除会产生矩形色块的中心阴影。硬币深色资源清理破碎边缘，并移除与硬币主体冲突的结果徽标。
- SoundPool 改用游戏音效流；过程音确认进入播放调度后才启动计时，结果音确认调度后才揭示结果，页面取消时停止当前短音效。
- 用户体验时长统一为：是/否 `1.4s`、硬币 `2.5s`、抽签 `3.0s`、罗盘 `3.6s`、指尖选择 `3.0s`。硬币动画覆盖前 `2.3s`，抽签余光覆盖剩余等待，避免动画停住后空等。
- 未改变路由、随机结果算法、历史记录或模板持久化。

## 2026-08-03 深色页面覆盖与柔和边框修复

- 首页六个工具、四个快捷模板以及抽签、指尖选择、硬币展示按主题切换深色资源，移除深色页面中的浅色画布。
- 重新生成抽签与指尖深色插画，清理透明化造成的缺口、硬切边和残留辅助线，并在深色模式使用 18vp 柔和圆角。
- 随机数字三个输入框在深色模式明确使用白色文字；深色共享描边从 `#294361` 降低为 `#203654`，浅色令牌不变。
- 未改动布局、路由、交互、随机算法、历史/模板持久化或反馈时序。

## 2026-08-03 夜间模式视觉还原

- 深色主题更新为参考稿的深海军蓝、蓝黑卡片、冷蓝描边和发光主操作层级，浅色主题不变。
- 转盘在深色模式使用高对比扇区和冷光中心；选项编辑、输入、标题、抽签/硬币展示与是或否按钮同步深色视觉。
- 未改动功能、随机算法、路由、模板/历史持久化或反馈时序。

## 2026-07-29｜设计稿 UI 还原

- 状态：`done`
- 用户请求：按提供的 UI 原稿完整复刻界面，保留现有功能。
- 项目类型：HarmonyOS Stage 模型 ArkTS/ArkUI。

### 本次改动

- 更新冷白背景、蓝色主操作、字体、间距、圆角、阴影和转盘色板令牌。
- 统一按钮、卡片、输入框、页头、选项编辑器和决策展示组件。
- 还原首页、转盘、抽签、指尖选择、抛硬币、是或否、随机数字、历史和设置。
- 从批准 JPG 确定性裁切首页、历史、抽签、指尖和硬币资源。
- 新增视觉令牌回归测试，并修复历史工具筛选到 `ToolType` 的映射。

### 验证

- 标准检查：通过，保留 `2in1` 和 `common/` 两条非阻断建议。
- 主 HAP：`BUILD SUCCESSFUL`，3,286,773 bytes。
- 测试 HAP：`BUILD SUCCESSFUL`，4,000,664 bytes。
- 设备测试：63 run，63 pass，Failure 0，Error 0。
- 设备截图：`docs/qa/screenshots/2026-07-29-*-final.jpeg`。

## 2026-07-29｜测试反馈修复

- 随机数字页放大结果并放松布局，快捷范围可直接同步最小值和最大值。
- 抛硬币页移除百分比统计卡片，新增可选问题输入；硬币动画与结果文字解耦。
- 历史页放大筛选和记录字号，并新增明显的“返回首页”按钮。
- 全局返回按钮统一为 48vp 热区和 34fp 箭头。
- 转盘标签使用清晰的最小字号，并通过响应式快照保证编辑、旋转和结果关闭后选项不丢失。
- 隐私说明页统一卡片、间距和正文行高；所有实际滚动容器隐藏滚动条。
- 选项列表移除嵌套滚动，页面采用整体滚动；键盘使用 `RESIZE` 避让底部操作。
- “谁去拿外卖”和“谁先开始”分别预设张三/李四/王五与玩家A/玩家B。

### 反馈验证

- 随机范围、硬币结果、键盘避让、转盘持久化和两个模板均完成设备真点击验证。
- 新增 4 项反馈回归测试，设备测试总数从 59 增至 63，全部通过。
- 截图证据：`docs/qa/screenshots/feedback-*.jpeg`。

### 遗留风险

- phone API 24 模拟器已验证；tablet 运行验证受可用设备限制，记录为 `blocked`。
- 派生插画包含批准浅色背景，深色主题保持可用但不属于逐像素验收目标。
- 构建使用 unsigned HAP，签名配置仍由现有发布流程负责。

## 2026-07-29｜抽签动效与转盘选项保留

- 抽签改为约 2 秒的五段衰减摇动、短暂停顿、签条超调上升和回落高亮。
- 抽签筒与签条使用独立变换层，签条文字始终正向可读且不侵入页头。
- 开始、末次摇动和结果分别使用 `18ms`、`28ms`、`70ms` 震动；开始与结果播放现有 WAV 音效。
- 音效失败增加无敏感内容的 `hilog.warn`，反馈失败不阻断决策流程。
- 转盘在转动和结果阶段使用稳定只读快照展示选项，并在结果完成、重试和关闭时恢复草稿，避免 TextInput 重建导致文字消失。
- 新增抽签时间线 3 项测试和转盘连续两次输入保留回归，设备测试由 63 项增至 67 项并全部通过。

## 2026-07-29｜随机数字预设单击修复

- 修复预设更新输入框时被 `onChange` 回调误标为“自定义”的状态覆盖。
- 输入框现在根据 min/max 反向匹配 `1–10`、`1–100`、`1–1000`，非预设值才选择“自定义”。
- 新增范围选中回归测试；主、测试 HAP 构建成功，设备测试 68/68 通过。
- 模拟器逐个单击验证：一次点击即可更新最大值并保持对应按钮高亮。

## 2026-07-29｜最终版本源码恢复

- 从本机原始 Codex 会话记录按时间顺序重放成功源码补丁，恢复误删的最终版本。
- 恢复范围覆盖共享组件、设计令牌、九个页面、抽签反馈、转盘选项保留和随机数字单击修复。
- 未覆盖用户现有的 `module.json5` 与图标配置修改。
- 标准检查、主 HAP、测试 HAP、68/68 设备测试和随机数字真机单击复测均通过。

## 2026-07-29｜应用图标透明边缘

- 将 `icon_1.png` 圆角外围的连通黑底转换为透明，画布保持 1024×1024。
- 对半透明边缘使用邻近主体颜色去除黑边和彩边，内部完全不透明主体像素保持不变。
- 输出包含 42,023 个全透明像素和 1,291 个抗锯齿半透明像素。
- 主 HAP 与测试 HAP 构建成功。

## 2026-07-30｜AppGallery UX 合规修复

- 保持业务功能、随机算法、路由和本地数据不变，主窗口启用沉浸式布局并动态监听系统状态栏与导航指示条避让区。
- 根 `Navigation` 消费上下安全区，底部固定操作、输入区域和滚动内容统一位于系统导航栏上方。
- 系统栏内容色跟随显式浅色/深色主题及系统颜色模式变化；深色背景使用浅色系统图标，窗口销毁时释放监听与窗口引用。
- 应用图标改为前景/背景 `layered-image`；前景使用用户指定的 `Gemini_Generated_Image_cy0zdgcy0zdgcy0z(1).png` 主体，背景为无圆角、满画布的不透明图层。
- AppScope 与 EntryAbility 均引用同一套 1024×1024 分层资源，启动窗口继续使用相同前景视觉。
- 新增 `scripts/check-appgallery-ux.ps1`，检查安全区调用链与实际 padding、主题同步、启动图标引用、透明角点、背景边界、分层描述及双份资源哈希。
- API 24 phone 模拟器完成首页、转盘首屏、滚动到底与深色主题截图；底部按钮完整位于 98px 手势导航区上方，69/69 设备测试通过。
- foldable/tablet 仅完成动态避让代码适配，因当前无对应设备，未记为设备验证通过。

## 2026-07-30｜滚动边界与宽屏约束补齐

- 首页、转盘、抽签、历史、隐私、随机数字和设置页的 `Scroll`/`Grid`/`List` 显式启用 Spring 边界反馈，滚动条继续隐藏。
- 已声明 tablet 的九个内页统一使用 720vp 最大内容宽度，导航目标在宽屏居中；phone 宽度小于上限，原布局与业务交互不变。
- 指尖选择保留全屏多指触摸区域，仅限制底部操作控件宽度，避免宽屏按钮横向拉满。
- API 24 phone 模拟器捕获到顶部回拉位移，随机数字和设置页底部内容均位于 98px 手势导航区上方。
- 标准检查、主/测试 HAP 构建及 69/69 设备测试通过；tablet/foldable 因无对应设备继续标记为 `blocked`。

## 2026-08-03｜第二版反馈能力与上架修复

- 修复设置页“振动反馈”“音效”开关只保存配置、开启后没有即时预览的问题；设置保存成功后分别触发一次短振动和 TAP 短音效，关闭开关不产生反馈。
- 将短音效播放从存在状态竞争的 AVPlayer 初始化链路改为面向短音效的 SoundPool，预加载原有 rawfile WAV，并在后台释放、回到前台时重新初始化。
- 为指尖选择和随机数字补齐结果音效，使六个决策工具均受统一音效开关控制；反馈失败只记录并提示，不阻断决策结果。
- 新增设置反馈控制器和 SoundService 生命周期测试，设备测试由 69 项增至 76 项，76/76 通过。
- 在 phone 模拟器完成设置开关关闭/开启、重启持久化、前后台恢复，以及转盘、抽签、硬币、是/否、随机数字各连续 3 次交互复测；日志未发现 SoundPool、振动服务或预览失败。
- 发布版本更新为 `1.0.1`（versionCode `1000001`、buildVersion `2`），目标 API 24、兼容 API 22；输出主/测试签名 HAP。
- 模拟器无法证明真实马达的物理触感，发布前仍应在 HarmonyOS 真机执行一次设置开关和指尖多触点振动验收。

## 2026-08-03｜指尖选择页导航与触控区优化

- 返回键改为复用共享 `PageHeader` 的卡片式视觉；新增可选尺寸与圆角参数，默认 48vp 保持其他页面不变，指尖选择页采用 44vp。
- 删除“小提示”，两个功能键贴近根导航容器已消费的动态导航安全区，较原布局下移约 5% 屏高。
- 多指识别从整页根容器移至可伸展的页面主体触控层；返回键和两个功能键不再参与手指识别。
- 新增 `scripts/check-finger-select-layout.ps1` 并纳入标准检查，覆盖共享返回键、44vp 紧凑尺寸、已删除提示与独立触控层。
- API 22 phone 模拟器完成浅色和深色截图；标准检查、主/测试 HAP 构建和 76 项设备测试均通过。
