# 任务清单

状态定义：`pending`、`in_progress`、`done`、`blocked`。

## 当前任务

### T-20260803-016 SDK 与默认反馈最终复核

- 状态：`done`
- 目标：确认 API 22 / HarmonyOS 6.0.2 原生运行契约，保留振动反馈，并将音效默认值设为关闭。

检查项：

- [x] 确认 target/compatible SDK 均为 `6.0.2(22)`，runtimeOS 为 `HarmonyOS`。
- [x] 确认 Stage、ArkTS、ArkUI 原生实现且无 WebView/跨端运行时。
- [x] 确认系统振动反馈存在、默认开启且受设置开关控制。
- [x] 为默认关闭音效补充先失败后通过的初始化、清除数据和回退设置测试。
- [x] 主/测试 HAP 构建、80 项设备测试和标准检查通过。

### T-20260803-015 全面视觉与音效时序审查修复

- 状态：`done`
- 目标：修复转盘指针几何、首页/历史图标伪影、硬币展示、历史页主题覆盖与全局结果音效延迟，并同步覆盖浅色模式；完成三轮独立自审。

检查项：

- [x] 逐项复现并记录五类问题根因，新增先失败后通过的回归检查。
- [x] 转盘指针严格对齐圆心垂线并指向顶部分隔线。
- [x] 首页与历史/设置图标移除贯穿折线、毛边和浅色画布，深浅主题均清晰。
- [x] 硬币展示重做为可辨识、完整、无破损伪影的正反面视觉。
- [x] 历史记录页完整消费主题资源与令牌，深浅模式一致可读。
- [x] 修正全局过程音与结果音生命周期，并将是/否、硬币、抽签、罗盘、指尖选择时长统一为 1.4/2.5/3.0/3.6/3.0 秒。
- [x] 完成需求逐条审、跨页面视觉审、构建/设备回归审三轮自审，并更新设计、变更与 QA 证据。

### T-20260803-014 深色页面覆盖与柔和边框修复

- 状态：`done`
- 目标：补齐深色模式下仍引用浅色插画的页面，确保随机数字输入为白色文字，并降低深色边框对比度；不改动布局、交互、随机算法或持久化。

检查项：

- [x] 新增深色资源引用、输入文字和边框令牌静态回归检查，并先验证其在修复前失败。
- [x] 首页、抽签、指尖选择与硬币展示按主题切换现有深色资源。
- [x] 随机数字三个输入框在深色模式明确使用白色文字。
- [x] 深色共享边框使用更低对比的冷蓝令牌，浅色主题保持不变。
- [x] 更新设计、变更与 QA 记录，执行标准检查和主/测试 HAP 构建；标准检查受既有 target API 配置不一致阻断并已记录。

### T-20260803-013 指尖选择页导航与触控区优化

- 状态：`done`
- 目标：在不改变指尖选择状态机、随机逻辑或路由的前提下，统一该页返回键视觉，移除小提示，将底部功能键安全地下移，并扩大手指识别区域。

检查项：

- [x] 返回键复用既有页面首视觉语言，并缩小为 44vp。
- [x] 删除小提示；两个功能键整体下移约 5% 屏高，始终位于动态系统导航安全区上方。
- [x] 手指识别仅覆盖页面主体，返回键和两个功能键保持独立交互。
- [x] 补充静态回归检查并更新设计、变更和 QA 证据。
- [x] 执行标准检查、主/测试 HAP 构建及可用设备验证。

### T-20260803-012 夜间模式视觉还原

- 状态：`done`
- 目标：仅调整深色主题的界面视觉，保持浅色主题、功能、路由、随机算法与本地数据不变。

检查项：

- [x] 深色主题令牌、共享控件、转盘与决策页面已接入冷蓝夜间视觉。
- [x] ThemeResolver 深色令牌测试按先失败、后通过完成。
- [x] 标准检查和主/测试 HAP 构建通过。
- [ ] 设备截图验证：`blocked`（本轮未启动可用 phone 模拟器）。

### T-20260803-011 第二版反馈能力与上架修复

- 状态：`done`
- 目标：定位并修复设置页开启音效/振动后无即时反馈的问题，校正短音效播放链路，补齐同类工具反馈并产出可复检的第二版本。

检查项：

- [x] 记录审核路径、代码数据流和根因证据。
- [x] 为设置开关预览、短音效生命周期和反馈覆盖补充回归测试。
- [x] 修复反馈实现并保持开关、后台清理、随机算法和持久化职责不变。
- [x] 更新版本号、变更记录、设计说明与 QA 证据。
- [x] 标准检查、主/测试 HAP 构建及与风险相称的设备复测通过。

### T-20260729-001 按批准设计稿还原应用界面

- 状态：`done`
- 目标：按 `decision_jpg` 的 12 张设计稿还原 phone 竖屏 UI，保留现有功能。
- 范围：视觉令牌、共享组件、九个页面、批准源图派生资源、构建和设备 QA。

检查项：

- [x] 审计相关代码、配置、设计稿和 HarmonyOS 规范。
- [x] 重构需求范围内的 UI，不修改业务状态机和持久化结构。
- [x] 标准检查通过。
- [x] 主 HAP 与测试 HAP 从 clean 状态构建成功。
- [x] phone 模拟器完成页面截图与 63 项 Hypium 设备测试。
- [x] 回填变更、设计、QA 和遗留风险。

### T-20260729-004 测试反馈问题修复

- 状态：`done`
- 目标：逐项解决 13 条 UI 与交互反馈，保留现有决策功能。

检查项：

- [x] 优化随机数字、历史记录、隐私说明和全局返回按钮视觉。
- [x] 抛硬币改为可选问题输入，并保持结果文字正向可读。
- [x] 修复快捷范围、模板默认值、转盘标签和旋转后选项持久化。
- [x] 选项编辑器改为整页滚动，键盘采用 `RESIZE` 避让。
- [x] 所有实际滚动容器隐藏滚动条。
- [x] phone 模拟器完成交互复测、截图和 63 项设备测试。

### T-20260729-005 抽签动效与反馈优化

- 状态：`done`
- 目标：实现已确认的“强仪式感”抽签动效，补齐音效和三段震动反馈，并回归转盘手动选项连续两次转动后仍保留。

检查项：

- [x] 新增抽签时间线与转盘二次转动回归测试。
- [x] 抽签筒与签条使用独立动画层，结果文字始终正向可读。
- [x] 音效、震动服从设置开关，页面离开或进入后台时清理延迟反馈。
- [x] 主 HAP、测试 HAP、完整设备测试与 phone 交互验收通过。
- [x] 更新变更记录和 QA 证据并重启应用供测试。

### T-20260729-006 随机数字预设单击修复

- 状态：`done`
- 目标：三个快捷范围单击一次即更新数值和选中状态。

检查项：

- [x] 输入变化按当前 min/max 匹配预设，不再无条件切换为“自定义”。
- [x] 主、测试 HAP 构建成功，68 项设备测试全部通过。
- [x] phone 模拟器逐个单击三个预设，数值与高亮状态均正确。

### T-20260729-007 最终版本源码恢复

- 状态：`done`
- 目标：从原始会话补丁精确恢复被误删的最终 UI 与交互源码。

检查项：

- [x] 按原时间顺序恢复全部受影响的页面、组件、令牌、服务和测试入口。
- [x] 保留现有新增资源、domain、测试以及用户的 module/icon 修改。
- [x] 标准检查、主/测试 HAP 构建和 68 项设备测试通过。
- [x] phone 模拟器复测随机数字预设单击行为。

### T-20260729-008 应用图标外围透明化

- 状态：`done`
- 目标：保持图标 1024×1024 尺寸及主体像素不变，将圆角外围黑色改为透明。

检查项：

- [x] 仅处理与画布边界连通的黑底及其抗锯齿过渡。
- [x] 输出为 RGBA PNG，四角 Alpha 为 0，完全不透明主体像素无颜色变化。
- [x] 主 HAP 与测试 HAP 资源编译、打包成功。

### T-20260730-009 上架 UX 合规修复

- 状态：`done`
- 目标：不改变业务功能，修复底部系统导航栏避让，并将应用图标改为 1024×1024 前景/背景分层资源。

检查项：

- [x] 固定控件、输入区域和弹层不与底部系统导航栏重叠。
- [x] 应用图标前景与背景分别为 1024×1024，背景满画布且不预裁圆角，前景未增加内间距。
- [x] 深浅主题切换与系统颜色模式变化会同步更新透明系统栏内容色。
- [x] 发布检查覆盖透明角点、背景边界、双资源哈希、启动图标引用和安全区实际消费。
- [x] 标准检查、主/测试 HAP 构建与 69 项设备功能回归通过。

## 后续任务

### T-20260729-002 tablet 设备回归

- 状态：`blocked`
- 目标：在 tablet 模拟器或真机验证滚动、长文本和触控目标。
- 前置条件：提供可用的 API 24 tablet 模拟器或真机。

### T-20260729-003 2in1 设备声明评估

- 状态：`pending`
- 目标：产品确认是否需要新增 `2in1` 支持；本轮不扩大发布设备范围。

### T-20260730-010 导航区与宽屏适配补齐

- 状态：`done`
- 目标：不改变业务功能，补齐所有滚动页面的边界反馈，并避免已声明 tablet 的内页内容在宽屏横向拉满。

检查项：

- [x] 所有 `Scroll`、`Grid`、`List` 显式启用 Spring 边界反馈并保持滚动条隐藏。
- [x] 所有内页在宽屏使用居中的最大内容宽度，phone 布局保持不变。
- [x] 标准检查、主/测试 HAP 构建和 phone 模拟器回归通过。
- [x] tablet 因无可用设备继续标记为 `blocked`，不虚报设备验收。
